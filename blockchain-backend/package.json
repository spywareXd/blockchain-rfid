{
  "name": "blockchain-rfid-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Hardhat + Express middleware for RFID identity verification",
  "main": "server/index.js",
  "scripts": {
    "compile": "node scripts/compile.js",
    "node": "hardhat node",
    "deploy:local": "hardhat run scripts/deploy.js --network localhost",
    "server": "node server/index.js",
    "start": "node server/index.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "ethers": "^6.13.4",
    "express": "^4.21.1"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.15"
  }
}
